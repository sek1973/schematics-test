{
    "$schema": "http://json-schema.org/schema",
    "$id": "SchematicsNewLibrary",
    "title": "Create New Library Schema",
    "type": "object",
    "properties": {
        "name": {
            "description": "The name of the library.",
            "type": "string"
        }
    },
    "required": [
        "name"
    ]
}
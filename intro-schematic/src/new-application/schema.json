{
    "$schema": "http://json-schema.org/schema",
    "$id": "SchematicsNewApplication",
    "title": "Create New Application Schema",
    "type": "object",
    "properties": {
        "name": {
            "description": "The name of the application.",
            "type": "string"
        }
    },
    "required": [
        "name"
    ]
}